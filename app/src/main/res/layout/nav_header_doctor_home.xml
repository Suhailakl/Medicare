<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="@dimen/nav_header_height"
    android:background="@drawable/side_nav_bar"
    android:gravity="bottom"
    android:orientation="vertical"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:theme="@style/ThemeOverlay.AppCompat.Dark">

    <de.hdodenhof.circleimageview.CircleImageView
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:id="@+id/nav_doc_profile_image"
        android:layout_marginTop="10dp"
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:src="@mipmap/ic_splash"
        app:civ_border_width="2dp"
        app:civ_border_color="#FF000000"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingTop="@dimen/nav_header_vertical_spacing"
        android:text="Android Studio"
        android:textAppearance="@style/TextAppearance.AppCompat.Body1"
        android:id="@+id/nav_doc_name"
        android:textSize="15dp"
        android:layout_marginLeft="24dp"
        android:layout_marginStart="24dp"
        android:layout_alignParentBottom="true"
        android:layout_toRightOf="@+id/nav_doc_profile_image"
        android:layout_toEndOf="@+id/nav_doc_profile_image"
        android:layout_marginBottom="20dp" />
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/Mob_num"
        android:textSize="20dp"
        android:id="@+id/nav_doc_id"
        android:layout_above="@+id/nav_doc_name"
        android:layout_alignLeft="@+id/nav_doc_name"
        android:layout_alignStart="@+id/nav_doc_name" />
    <ImageButton
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:src="@mipmap/ic_edit"
        android:id="@+id/nav_doc_edit_image"
        android:layout_alignBottom="@+id/nav_doc_name"
        android:layout_alignRight="@+id/nav_doc_profile_image"
        android:layout_alignEnd="@+id/nav_doc_profile_image"
        android:layout_marginRight="20dp"
        android:layout_marginEnd="20dp" />

    <ProgressBar
        android:id="@+id/nav_doc_pro"
        style="?android:attr/progressBarStyle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/nav_doc_edit_image"
        android:layout_alignRight="@+id/nav_doc_edit_image"
        android:layout_alignEnd="@+id/nav_doc_edit_image"
        android:layout_marginRight="10dp"
        android:layout_marginEnd="10dp"
        android:layout_marginBottom="13dp" />

</RelativeLayout>
